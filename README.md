# Rust Webserver for ARM

This is a very simple rust webserver example built for use on small ARM devices (though it runs fine on large devices too).
resin.io is supported for remote building and deploying.
